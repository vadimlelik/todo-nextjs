name: CI/CD Next.js App

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Клонируем репозиторий
      - name: Checkout
        uses: actions/checkout@v4

      # 2️⃣ Вход в Docker Hub
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      # 3️⃣ Сборка и пуш Docker-образа Next.js
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/todo-nextjs:latest
          build-args: |
            MONGODB_URI=${{ secrets.MONGODB_URI }}

      # 4️⃣ Упаковываем все deployment-файлы в архив
      - name: Pack deployment files
        run: |
          tar czf deploy.tar.gz docker-compose.yml nginx/conf.d certs

      # 5️⃣ Загружаем архив на сервер
      - name: Upload deploy archive
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: deploy.tar.gz
          target: /apps/todo-nextjs/

      # 6️⃣ Деплой на сервере через SSH
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            set -e
            cd /apps/todo-nextjs

            # Распаковываем файлы
            tar xzf deploy.tar.gz
            rm deploy.tar.gz

            echo "🔁 Pulling latest Docker image..."
            docker pull ${{ secrets.DOCKER_USERNAME }}/todo-nextjs:latest

            echo "🛑 Stopping old containers..."
            docker compose down || true

            echo "🚀 Starting new version..."
            export MONGODB_URI=${{ secrets.MONGODB_URI }}
            docker compose up -d --build

            echo "✅ Deployment completed!"
